export const selectUserPosts = (state) => state.posts.userPosts;
export const selectAllPosts = (state) => state.posts.allPosts;
export const selectComments = (state) => state.posts.comments;
export const selectIsRefresing = (state) => state.posts.isRefresing;
export const selectLikes = (state) => state.posts.likes;
